{
  "name": "adamrahmata",
  "private": true,
  "description": "Pintasan agar npm run dev / build bisa dijalankan dari akar repo, bukan hanya dari dalam site/.",
  "scripts": {
    "dev": "npm --prefix site run dev",
    "build": "npm --prefix site run build",
    "preview": "npm --prefix site run preview",
    "check": "npm --prefix site run check",
    "setup": "npm --prefix site install"
  }
}
